= Worker Tracker

a generic platform for worker monitoring and management.

== Description

worker_tracker provides these functions
* list available workers
* add available workers
* mark workers as busy
* mark workers as unreachable because of heartbeat timeout
